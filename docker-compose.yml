services:
  web:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - .:/workspaces/new-rails-asyut
    working_dir: /workspaces/new-rails-asyut
    ports:
      - "3000:3000"
    command: bash
    stdin_open: true
    tty: true
